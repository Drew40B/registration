{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/pannel/pannel.api/pannel.api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}